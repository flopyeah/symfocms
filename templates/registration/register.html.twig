{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

    <h1>Register</h1>

    {{ form_start(registrationForm, {'action' : path('app_register'), 'attr' : { 'id' : 'registration_form' }}) }}
        {{ form_row(registrationForm.email, { 'attr': {'class': 'input'} , 'label' : 'E-mail'}) }}
        {{ form_row(registrationForm.plainPassword, { 'attr': {'class': 'input'} , 'label' : 'Mot de passe'}) }}
        {{ form_row(registrationForm.firstname, { 'attr': {'class': 'input'} , 'label' : 'Prénom'}) }}
        {{ form_row(registrationForm.lastname, { 'attr': {'class': 'input'} , 'label' : 'Nom'}) }}

        <button class="button submit is-fullwidth is-rounded">Register</button>
    {{ form_end(registrationForm) }}

{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script>
        console.log('submit handle')
    </script>

{% endblock %}